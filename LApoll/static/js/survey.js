/*
document.querySelectorAll('.questionRow').forEach(x=> x.addEventListener('mousemove', (event)=>event.target.style.scale=2))
document.querySelectorAll('.questionRow').forEach(x=> x.addEventListener('mouseout', (event)=>event.target.style.scale=1))
*/
